import Flutter
import UIKit

/// Required by the UIScene lifecycle, which is mandatory on iOS 27 and later.
///
/// `FlutterSceneDelegate` does all the work of attaching the Flutter engine to
/// the connected scene; this subclass exists so Info.plist has a concrete class
/// to name in `UISceneDelegateClassName`.
class SceneDelegate: FlutterSceneDelegate {}
